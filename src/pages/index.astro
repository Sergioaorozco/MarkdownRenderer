---
const isMDX = false;
---

 <main class="wrapper text-center py-10 flex justify-center flex-col gap-y-10 w-full">
  <h1 class="text-slate-700 text-4xl font-bold">Markdown to <span class="text-slate-500 italic">Formatted Text</span></h1>
  <div class="flex gap-x-4 justify-center mt-5">
    <textarea name="textInit" id="textInit" cols="30" rows="10" placeholder="Markdown text" class="border border-slate-400 rounded-lg p-2 w-full"></textarea>
    <textarea name="outputText" id="outputText" cols="30" rows="10" class="border border-slate-400 rounded-lg bg-slate-200 p-2 w-full" readonly></textarea>
  </div>
  <button class="bg-orange-600 hover:bg-orange-700 transition-colors duration-200 text-white py-3 rounded-lg">{!isMDX ? 'No hay information' : 'Convertir Textos'}</button>
 </main>

<style>
  .wrapper {
    max-inline-size: 1125px;
    margin: 0 auto;
  }

</style>
